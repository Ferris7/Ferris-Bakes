@using Microsoft.AspNetCore.Identity
@using Ferris_Bakes.Models
@using Ferris_Bakes.Areas.Identity.Pages

@inject SignInManager<UserModel> SignInManager
@inject UserManager<UserModel> UserManager

<div class="right">
    @if (SignInManager.IsSignedIn(User))
    {
        <a asp-area="Identity" asp-page="/Account/Manage/Index"><img src="~/pics/profile.png" alt="profile" style="width:42px;height:42px;"></a>
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button class="btn btn-info" type="submit" id="betterButton">Logout</button>
        </form>

    }
    else
    {
        <!--
        <a class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>

           <form id="login" class="form-inline" asp-area="Identity" asp-page="/Account/Login">
            <button class="btn btn-info" type="submit" id="betterButton">Login</button>
        </form>
            <a class="btn btn-info" id="betterButton" asp-controller="Account" asp-action="Login">Login</a>
        -->
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>


    }
</div>
